from setuptools import find_packages, setup

setup(
    name='fuzzydeduplication',
    packages=find_packages(),
    version='0.1.0',
    description='Library for fuzzy deduplication',
    author='AlexisOMG',
    license='MIT',
    install_requires=[],
)